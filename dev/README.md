# dev/

Developer-only materials excluded from the built R package (via
`.Rbuildignore`). Contains documentation strategy, audit plans,
numerical analyses, test fixtures, and design notes. Nothing in
this directory is shipped to CRAN or installed by users.
